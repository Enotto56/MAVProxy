# Agent Instructions

- Run `python -m compileall` only when explicitly requested.
- Prefer using `pytest` for Python testing when a test suite is required; fall back to `python -m unittest` if pytest is unavailable.
- Format any added or modified Python code with `black` when formatting is requested.
- Use descriptive commit messages summarizing the changes and affected modules.
